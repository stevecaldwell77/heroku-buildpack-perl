#!/bin/sh

cat << EOF
---
addons:
config_vars:
  PATH: local/bin:/usr/local/bin:/usr/bin:/bin
  PERL5LIB: /app/local/lib/perl5
default_process_types:
  web: perl -I\$PERL5LIB local/bin/carton exec starman --preload-app
EOF
